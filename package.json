{
  "name": "chrome-ui5-odata-viewer",
  "version": "1.0.0",
  "repository": "https://github.com/mxschmitt/chrome-odata-viewer",
  "author": "Max Schmitt",
  "description": "Displays the UI5 OData network requests and response payload in a beautified clickable way.",
  "license": "MIT",
  "dependencies": {
    "browserify": "^16.2.0",
    "json-tree-view": "^0.4.9",
    "key-del": "^1.3.0",
    "semantic-ui-css": "^2.3.1",
    "shx": "^0.2.2"
  },
  "scripts": {
    "install": "browserify -d -e tab.js -o dist/tab.js && browserify -d -e devtools.js -o dist/devtools.js && shx rm _config.yml"
  }
}
